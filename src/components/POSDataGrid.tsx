import React from "react";

export default function POSDataGrid() {
  return (
    <div className="p-4 text-sm">
      POS view not wired yet. (Next: add <code>/api/pos</code> and connect this grid.)
    </div>
  );
}
